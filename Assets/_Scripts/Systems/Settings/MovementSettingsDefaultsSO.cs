using UnityEngine;
using static PlayerSettings;

[CreateAssetMenu(fileName = "Movement Default Settings", menuName = "Default settings / New Movement Default Settings")]
public class MovementSettingsDefaultsSO : ScriptableObject
{
    public MovementSettings settings;
}
